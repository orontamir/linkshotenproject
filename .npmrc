; Run npm scripts through PowerShell instead of cmd.exe.
; The project path contains "&" which cmd.exe misinterprets as a command separator,
; breaking script execution. PowerShell quotes paths correctly and avoids this.
script-shell = powershell.exe
